#!/usr/bin/env bash
# Blocks pushing if package-lock.json / .npmrc resolve against a private
# registry (see scripts/check-registry.mjs for why this matters).
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
node scripts/check-registry.mjs
